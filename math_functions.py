#import first the math module to make math functions available
import math

pi =3.14
print(round(pi))
print(math.ceil(pi))
print(math.floor(pi))
print(abs(pi))
print(pow(pi, 2))
#basically all these functions and many others are available due to the math module

x, y, z = 20, 134, 3
print(max(x, y, z))
print(min(x, y, z))